import React from 'react';
import toJson from 'enzyme-to-json';
import { mount } from 'enzyme';

import App from './App';

describe('App', () => {
  // TODO implement the tests. Feel the pain.

  it.skip('render no posts', () => {
    // const wrapper = mount(<App />);
    // expect(toJson(wrapper)).toMatchSnapshot();
  });

  it.skip('render the posts', () => {
    // const posts = [{id: 1, title: "json-server", author: "typicode"}];
    // const wrapper = mount(<App />);
    // expect(toJson(wrapper)).toMatchSnapshot();
  });
});

